---
description: Kill a process running on a specific port (default 3000)
---

// turbo-all

1. Kill the process on the specified port (default is 3000):
```bash
npx kill-port <port>
```

2. Confirm the process was killed.
