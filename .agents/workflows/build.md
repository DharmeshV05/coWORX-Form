---
description: Build the project for production and check for errors
---

// turbo-all

1. Run the production build:
```bash
npm run build
```

2. Check the build output for any errors or warnings.

3. If there are TypeScript or build errors, identify the failing files and fix them.

4. Re-run the build if fixes were applied to confirm everything passes.
