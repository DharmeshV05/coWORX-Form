---
description: Add a new shadcn/ui component to the project
---

// turbo-all

1. Install the shadcn/ui component:
```bash
npx shadcn@latest add <component-name>
```

2. The component will be added to `src/components/ui/`.

3. Import it using the `@/components/ui/<component-name>` path alias.
